version: '3.8'

services:
  google-maps-scraper:
    image: gosom/google-maps-scraper:latest
    restart: unless-stopped
    expose:
      - "8080"
    volumes:
      - scraper_data:/data
    command: ["-web", "-dsn", "/data/leads.db"]

volumes:
  scraper_data:
    driver: local
